diff --git a/src/main/fc/config.c b/src/main/fc/config.c
index a1b2c3d..e4f5g6h 100644
--- a/src/main/fc/config.c
+++ b/src/main/fc/config.c
@@ -123,6 +123,12 @@
     .max_angle_inclination = 500,
+    .gyro_main_lpf_hz = 25,
+    .dynamic_gyro_notch_q = 250,
+    .dynamic_gyro_notch_min_hz = 30,
+    .small_angle = 180,
+    .deadband = 32,
+    .airmode_type = STICK_CENTER_ONCE,
 
@@ -130,6 +136,15 @@
     .max_angle_inclination_rll = 450,
+    .failsafe_procedure = NONE,
+    .disarm_kill_switch = OFF,
+    .max_throttle = 2000,
+    .motor_pwm_protocol = STANDARD,
+    .throttle_idle = 5.0,
 
@@ -200,6 +215,12 @@
     .gps_sbas_mode = AUTO,
+    .gps_ublox_use_galileo = ON,
+    .gps_ublox_use_beidou = ON,
+    .gps_ublox_use_glonass = ON,
 
@@ -250,6 +271,20 @@
     .nav_rth_altitude = 5000,
+    .nav_wp_radius = 5000,
+    .nav_wp_max_safe_distance = 500,
+    .nav_rth_allow_landing = FS_ONLY,
+    .nav_fw_climb_angle = 12,
+    .nav_fw_control_smoothness = 2,
+    .nav_fw_launch_motor_delay = 1,
+    .nav_fw_launch_spinup_time = 250,
+    .nav_fw_launch_end_time = 5000,
+    .nav_fw_launch_timeout = 50000,
+    .nav_fw_launch_max_altitude = 5000,
+    .nav_fw_launch_climb_angle = 15,
+    .nav_extra_arming_safety = ON,
 
@@ -300,6 +335,15 @@
     .fw_p_pitch = 15,
+    .fw_i_pitch = 5,
+    .fw_d_pitch = 5,
+    .fw_ff_pitch = 80,
+    .fw_p_roll = 13,
+    .fw_i_roll = 3,
+    .fw_turn_assist_yaw_gain = 2.0,
+    .fw_turn_assist_pitch_gain = 0.6,
+    .nav_fw_pos_z_p = 25,
+    .nav_fw_pos_z_d = 8,
+    .nav_fw_pos_xy_p = 55,
 
@@ -350,6 +394,15 @@
     .rc_expo = 30,
+    .rc_yaw_expo = 30,
+    .roll_rate = 18,
+    .pitch_rate = 9,
+    .yaw_rate = 3,
+    .dterm_lpf_hz = 10,
+    .d_boost_min = 1.0,
+    .d_boost_max = 1.0,
+    .tpa_rate = 5,
+    .tpa_breakpoint = 1750,
